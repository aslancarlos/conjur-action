name: conjur-action

on:
  workflow_call:
    secrets:
      SQL_USERNAME:
        description: 'CONJUR SECRET SQL USERNAME'
        required: true
      SQL_PASSWORD:
        description: 'CONJUR SECRET SQL PASSWORD'
        required: true
  push:
    branches:
      - master
  pull_request:

jobs:
  authn-jwt:
    runs-on: self-hosted
    
    permissions:
      id-token: 'write'
      contents: 'read'

    steps:
      - uses: actions/checkout@v1
      
      - name: Use CyberArk Conjur Secret Fetcher Action w/ GitHub JWT
        id: conjur-secrets
        uses: ./  # Repositório local ou ação personalizada
        with:
          url: https://latamlab.secretsmgr.cyberark.cloud/api
          account: conjur
          authn_id: github
          secrets: data/vault/dev-demo-cred/appuser_db/username|SQL_USERNAME;data/vault/dev-demo-cred/appuser_db/password|SQL_PASSWORD
          
      - name: Clean Workspace
        uses: AutoModality/action-clean@v1.1.0

  check_mysql:
    runs-on: self-hosted
    needs: authn-jwt

    steps:
      - name: Print Output Variables from authn-jwt in check_mysql
        run: |
          echo "SQL_USERNAME: ${{ secrets.SQL_USERNAME }}"
          echo "SQL_PASSWORD: ${{ secrets.SQL_PASSWORD }}"

      - name: Check MySQL Connection
        run: |
          echo "Verificando conexão com o banco de dados..."
          echo "Usando usuário: ${{ secrets.SQL_USERNAME }}"
          mysql -h 10.78.10.171 -u "${{ secrets.SQL_USERNAME }}" -p"${{ secrets.SQL_PASSWORD }}" -e "SHOW PROCESSLIST;" || exit 1
